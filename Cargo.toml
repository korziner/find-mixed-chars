[package]
name = "find-mixed-chars"
edition = "2024"
description = "Обнаруживает латиницу, цифры и нехарактерные символы внутри русских слов"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
encoding_rs = "0.8"
encoding_rs_io = "0.1"
rayon = "1.12"
termcolor = "1.4"
